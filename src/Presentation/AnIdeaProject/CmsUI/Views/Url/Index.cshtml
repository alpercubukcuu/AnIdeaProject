@using Application.Dto
@model List<UrlRecordDto>



<div>
    <a class="btn btn-success" href="/AddUrl">Add Url</a>

    @if (Model.Count() > 0)
    {
        @foreach (var item in Model)
        {
            <p>@item.Path</p>
        }
    }
   
</div>